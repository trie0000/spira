{
  "name": "spira",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "SharePoint 上で動くメール起票型チケット管理システム (bookmarklet)",
  "scripts": {
    "dev": "node build.js --watch --serve",
    "build": "node build.js",
    "build:prod": "node build.js --prod",
    "build:bookmarklet": "node build.js --bookmarklet",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "esbuild": "^0.21.0",
    "typescript": "^5.4.0"
  },
  "dependencies": {
    "@kenjiuno/msgreader": "^1.28.0",
    "dompurify": "^3.1.0"
  }
}
