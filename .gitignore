node_modules/
# Build artifacts: keep the standalone HTML (used by users to install /
# open Spira) under version control; ignore the regenerable JS + sourcemap.
dist/*
!dist/index.html
!dist/install.html
!dist/bookmarklet.txt
*.log
.DS_Store
.vscode/
unpacked/
Spira.zip

# AI relay の個人設定 (テンプレートは .example として残す)
scripts/spira-ai-relay.env
